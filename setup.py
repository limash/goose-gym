from setuptools import setup

setup(name='gym_goose',
      version='0.0.1',
      install_requires=['gym',
                        'kaggle-environments',
                        'numpy',
                        'tensorflow']
      )
